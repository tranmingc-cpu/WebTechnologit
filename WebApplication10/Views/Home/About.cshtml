@model WebApplication10.Models.AboutPage

@{
    ViewBag.Title = Model?.Title ?? "Giới thiệu";
}

<section class="about-page">
    <div class="section-header">
        <h2>@ViewBag.Title</h2>
    </div>

    <div class="about-content">
        @Html.Raw(Model?.Description)
    </div>
</section>